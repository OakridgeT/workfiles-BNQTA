schema_version: "1.0"
ai_orientation:
  purpose: "Machine-readable interpretation metadata for Figure 6.2-b."
  source_markdown: "Tables_related.md"
  source_image: "6.2-b.png"
figure:
  id: "6.2-b"
  type: "timing_waveform"
  title: "VOLTAGE WAVEFORMS — PROPAGATION DELAY TIMES INVERTING AND NONINVERTING OUTPUTS"
  parameters: ["tPLH", "tPHL"]
  equivalent_parameter: "tpd"
visual_semantics:
  signal_relationship: "The figure illustrates propagation delay between an input transition and the corresponding output transition for inverting and noninverting output behavior."
  visual_grounding: "Use the PNG to determine polarity, transition direction, reference threshold VM, and the start/end points of the timing arrows."
timing:
  tPLH:
    definition: "Propagation delay associated with output low-to-high transition."
    equivalent: "tpd"
  tPHL:
    definition: "Propagation delay associated with output high-to-low transition."
    equivalent: "tpd"
conditions:
  VCC_options: ["1.8 V ± 0.15 V", "2.5 V ± 0.2 V", "3.3 V ± 0.3 V", "5 V ± 0.5 V"]
  temperature_ranges: ["-40°C to +85°C", "-40°C to +125°C"]
  CL: ["30 pF", "50 pF"]
measurement_rules:
  - "Propagation delay is measured between the specified input and output reference points."
  - "VM is the midpoint reference used for the timing measurement according to the load-condition table."
  - "Outputs are measured one at a time, with one transition per measurement."
  - "Do not confuse tPLH/tPHL with setup or hold time."
ai_interpretation_rules:
  - "Identify input transition first, then identify the corresponding output transition."
  - "Use waveform polarity shown in the PNG; do not assume that every device has the same logical polarity."
  - "Use the timing tables for guaranteed MIN/MAX values; use the PNG for visual timing relationships."
